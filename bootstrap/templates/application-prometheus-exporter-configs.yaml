{{- $app := index .Values.applications "prom_exporter_configs" }}
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: prometheus-exporter-configs
  namespace: openshift-gitops
spec:
  project: default
  {{- include "app.source" (dict "root" $ "app" $app) | nindent 2 }}
    helm:
      values: |
        mykey: myvalue
  destination:
    server: https://kubernetes.default.svc
    namespace: {{ $app.destination.namespace }}
  syncPolicy:
    automated: {}
